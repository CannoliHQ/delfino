# sdk=34 because delfino's compileSdk (37) is newer than any released Robolectric supports.
sdk=34

# The stock Application replaces DolphinApplication, whose onCreate loads the native "main"
# library that does not exist on a JVM test's library path.
application=android.app.Application
